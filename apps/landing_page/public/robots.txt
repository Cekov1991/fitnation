# Fit Nation — https://joinfitnation.com
# Everything here is public marketing content. All crawlers are welcome,
# including AI crawlers: assistant answers are a real discovery channel for a
# consumer app, so being readable matters more than being withheld.

User-agent: *
Allow: /

# --- Cloudflare Content Signals Policy -------------------------------------
# Separates what a crawler may do with the content from whether it may fetch it.
# Ignored by crawlers that do not implement it; costs nothing when unsupported.
# search    = may appear in search results
# ai-input  = may be retrieved as grounding for a generated answer (RAG)
# ai-train  = may be used to train a model
Content-Signal: search=yes, ai-input=yes, ai-train=yes

# --- AI crawlers, named explicitly ----------------------------------------
# `User-agent: *` above already permits these. They are listed so the intent is
# unambiguous and so a future change is a one-line edit per agent rather than a
# research exercise. Split by what the agent actually does.

# Fetches to ground a live answer, and/or drives search surfaces
User-agent: OAI-SearchBot
User-agent: ChatGPT-User
User-agent: Claude-SearchBot
User-agent: Claude-User
User-agent: PerplexityBot
User-agent: Perplexity-User
User-agent: MistralAI-User
Allow: /

# Bulk crawlers; corpora feed both retrieval and training
User-agent: GPTBot
User-agent: ClaudeBot
User-agent: Google-Extended
User-agent: Applebot-Extended
User-agent: CCBot
User-agent: meta-externalagent
User-agent: Bytespider
User-agent: Amazonbot
User-agent: cohere-ai
User-agent: AI2Bot
User-agent: Diffbot
User-agent: Omgilibot
Allow: /

Sitemap: https://joinfitnation.com/sitemap.xml
